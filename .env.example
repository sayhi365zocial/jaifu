# Base URL of the KandA backend API, including /api.
# Leave unset in local dev — vite proxies /api to http://localhost:5000.
VITE_API_BASE_URL=https://your-backend.up.railway.app/api

# Where the "ติชม" button points (LINE OA, Google Form, ...).
# Leave unset to use the default: mailto:sayhi@365zocial.com
VITE_FEEDBACK_URL=
